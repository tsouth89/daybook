[package]
name = "daybook-mcp"
version = "0.1.0"
edition = "2021"
description = "MCP server for Daybook. Speaks JSON-RPC on stdio."

[[bin]]
name = "daybook-mcp"
path = "src/main.rs"

[dependencies]
daybook-core = { path = "../core" }
serde_json = "1"
chrono = { version = "0.4" }
